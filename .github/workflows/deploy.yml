name: 🚀 Auto Deploy Backend (Self-Hosted)

on:
  push:
    branches:
      - master

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: 🚀 Deploy Backend
        run: |
          bash ~/vms/deploy.sh
