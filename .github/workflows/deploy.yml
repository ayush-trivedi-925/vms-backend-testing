name: 🚀 Full VMS Deployment (Frontend + Backend)

on:
  push:
    branches:
      - master

jobs:
  deploy:
    runs-on: [self-hosted, Linux, X64]

    steps:
      - name: 📥 Checkout Backend Repo
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 🚀 Run Deployment Script
        env:
          HOME: /home/ubuntu
        run: |
          echo "==============================="
          echo "🚀 VMS Deployment Started"
          echo "==============================="
          bash /home/ubuntu/vms/deploy.sh
          echo "✅ VMS Deployment Finished"
